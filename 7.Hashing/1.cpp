///// hashing intro and application /////

/*
    useful : search, insert, delete in O(1) on avg
    unuseful : (finding closest value, sorted data)avl tree or red black tree, (prefix searching)trie

    application
    > implementation of dictionaries in constant time
    > database indexing
    > cryptography : a decent website will never store pswd in plane text, they generate a hash out of it and store that.
    > caches
    > symbol tables in compilers / interpreters
    > routers
    > getting data from databases
    > many more
*/

#include<bits/stdc++.h>
using namespace std;

int main()
{
    
}